# セキュリティ設定
# Security configurations

# 公式npmレジストリのみ使用
# Use official npm registry only
registry=https://registry.npmjs.org/

# package-lock.jsonを必ず生成
# Always generate package-lock.json
package-lock=true

# インストール時にスクリプトを実行しない（セキュリティ対策）
# Do not run scripts during install (security measure)
ignore-scripts=true

# 監査レベル: moderate以上の脆弱性で警告
# Audit level: warn on moderate or higher vulnerabilities
audit-level=moderate
