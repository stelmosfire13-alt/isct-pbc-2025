# **チーム開発のためのGit実践ガイド**

このドキュメントでは、チーム開発において頻出するGitの操作、運用ルール、トラブルシューティングについて解説します。

## **1\. 開発を始める準備 (Clone)**

プロジェクトに参加して最初に一度だけ行う作業です。リモートリポジトリ（GitHubなど）にあるコードを自分のPC（ローカル環境）にコピーします。

### **手順**

1. **リポジトリURLの取得:** GitHubなどの画面にある「Code」ボタンから、リポジトリのURL（HTTPSまたはSSH）をコピーします。  
2. **コマンド実行:** ターミナルで保存したいディレクトリに移動し、以下のコマンドを実行します。  
   \# git clone \[リポジトリのURL\]  
   git clone \[https://github.com/organization/project-name.git\](https://github.com/organization/project-name.git)

3. **ディレクトリ移動:** cloneするとプロジェクト名のフォルダが作成されるので、その中に移動します。  
   cd project-name

## **2\. 基本的な開発サイクル**

チーム開発では、原則として\*\*「メインのブランチ（main/develop）を直接編集しない」\*\*ことが鉄則です。

### **基本フロー**

1. 最新の状態を取得する  
   作業を始める前は必ずmainブランチを最新にします。  
   git checkout main  
   git pull origin main

2. 作業用ブランチを作成して移動する  
   自分専用の作業ブランチを切ります。  
   \# git checkout \-b \[ブランチ名\]  
   git checkout \-b feat/add-login-form

3. 作業を行い、ステージング・コミットする  
   慣れるまでは git add . の前に git status を実行し、意図しないファイルが含まれていないか確認する癖をつけましょう。  
   git status       \# 変更ファイルの確認  
   git add .        \# 変更をステージング  
   git commit \-m "feat: ログイン画面のレイアウト作成"

4. **リモートリポジトリへプッシュする**  
   git push origin feat/add-login-form

## **3\. マージ (Merge) と リベース (Rebase)**

チーム開発において、他のメンバーの変更を取り込んだり、自分の変更を統合する際のアプローチです。

### **Merge（マージ）**

履歴を「合流」させる方法です。分岐した履歴がそのまま残り、合流点に「マージコミット」が作られます。

* **メリット:** 実際に何がいつ起きたかの履歴が正確に残る。コンフリクト解消が1回で済む。  
* **デメリット:** ブランチの履歴が複雑になりがち（トポロジカルに汚くなる）。  
* **いつ使う？:** main や develop ブランチへの統合時（プルリクエストのマージ時など）。

\# mainブランチに featureブランチを取り込む場合  
git checkout main  
git merge feature-branch

### **Rebase（リベース）**

履歴を「付け替える」方法です。自分の作業の「ベース（出発点）」を最新の状態に移動させます。

* **メリット:** 履歴が一直線になり、非常に綺麗で見やすい。  
* **デメリット:** 履歴を改変するため、**すでにPushした（他人と共有している）ブランチで行うと整合性が壊れる**。  
* **いつ使う？:** 自分のローカル作業ブランチを、最新の main の状態に追従させたい時。

\# 自分の作業ブランチ(feature)にいて、mainの最新を取り込みたい場合  
git checkout feature-branch  
git rebase main

**⚠️ 注意:** git rebase は「履歴の書き換え」です。チームメンバーと共有しているブランチ（developなど）に対しては絶対に行わないでください。

## **4\. コンフリクト（競合）時の対応**

同じファイルの同じ行を、別の人が編集していた場合に発生します。**慌てる必要はありません。**

### **解消手順**

1. **状況確認:** git status で競合しているファイルを確認します。  
2. **ファイル編集:** 該当ファイルを開くと、以下のような記号が入っています。  
   \<\<\<\<\<\<\< HEAD  
   あなたが変更したコード  
   \=======  
   相手（取り込もうとしているブランチ）が変更したコード  
   \>\>\>\>\>\>\> develop

3. **修正:** \<\<\<\<\<\<\<, \=======, \>\>\>\>\>\>\> をすべて削除し、最終的にあるべきコードの形に修正します。  
4. **コミット:**  
   git add \[ファイル名\]  
   \# mergeの場合  
   git commit \-m "fix: コンフリクト解消"

   \# rebase中の場合  
   git rebase \--continue

## **5\. コミットメッセージの書き方**

「何をしたか」が一目でわかるようにルールを統一します。\*\*Prefix（接頭辞）\*\*をつけるのが一般的です。

### **推奨フォーマット**

\[Prefix\]: \[変更内容の要約\]

### **主なPrefix一覧**

| Prefix | 意味 | 使用例 |
| :---- | :---- | :---- |
| **feat** | 新機能の追加 | feat: ユーザー登録機能の実装 |
| **fix** | バグ修正 | fix: ログイン時にクラッシュする不具合を修正 |
| **refactor** | リファクタリング | refactor: ユーザーモデルのコード整理（機能変更なし） |
| **docs** | ドキュメントのみ | docs: READMEにセットアップ手順を追記 |
| **style** | コード整形 | style: インデントの修正 |
| **chore** | 雑務・設定変更 | chore: パッケージの依存関係更新 |

### **良いメッセージのコツ**

* **1行目は簡潔に:** 50文字以内で概要を書く。  
* **詳細が必要なら空行を挟んで書く:**  
  fix: 検索機能のバグ修正

  検索キーワードが空の状態で検索ボタンを押すと  
  エラーが発生していたため、バリデーションを追加。

## **6\. プルリクエスト (Pull Request / PR)**

コードレビューを依頼するための機能です（GitHub/GitLab等の機能）。

### **PR作成時のチェックリスト**

1. **タイトル:** \[Prefix\] をつけて内容を明確にする（例: feat: 商品一覧ページの作成）。  
2. **概要:** 「何をしたか」「なぜしたか」を書く。  
3. **動作確認方法:** レビュアーがどうやって動作を確認すればいいか書く（例: npm start して /products にアクセス）。  
4. **セルフレビュー:** PRを出す前に自分のコードを自分で見直し、不要なログ出力やタイポがないか確認する。  
5. **スクリーンショット:** UI変更がある場合はBefore/Afterの画像を貼る。

### **レビューを受ける側の心得**

* 指摘は「コードへの指摘」であり「人格への攻撃」ではないと心得る。  
* 議論が必要な場合は、チャットや対面で話してから結論をコメントに残す。

### **マージの種類（GitHub等の設定）**

チームの方針によりますが、現在は以下が主流です。

* **Squash and Merge:** PR内の複数のコミットを1つにまとめてマージする。mainブランチの履歴がスッキリする。  
* **Create a merge commit:** そのままマージする。詳細な履歴を残したい場合に使う。

## **7\. その他 便利コマンド集**

* # **作業の退避 (Stash)**   **作業途中で別のブランチに切り替えたいが、コミットはしたくない時。**   **git stash save "作業途中" git stash list \# 退避したリストを確認 git stash pop \# 元に戻す**

* 直前のコミット修正 (Amend)  
  コミットメッセージを間違えた、ファイルを追加し忘れた時（Push前限定）。  
  git add \[忘れ物\]  
  git commit \--amend

* **ログを綺麗に見る**  
  git log \--graph \--oneline \--all  
